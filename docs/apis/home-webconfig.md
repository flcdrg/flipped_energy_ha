# GET /home/webconfig

Purpose:

- Returns frontend/web configuration consumed by the portal app.

URL:

- <https://api.flipped.energy/home/webconfig>

Auth:

- Bearer token may be present in session, but endpoint is part of frontend bootstrap traffic.

Request:

- Method: GET

Observed status:

- 200

Notes:

- Current integration does not consume this endpoint.
