# GET /crisp/hmac

Purpose:

- Returns an HMAC payload for Crisp chat integration in the web app.

URL:

- <https://api.flipped.energy/crisp/hmac>

Auth:

- Bearer token/session context as used by the web app.

Request:

- Method: GET

Observed status:

- 200

Notes:

- This endpoint is not used by the Home Assistant integration.
