[project]
name = "flipped-energy-ha"
version = "0.1.1"
requires-python = ">=3.14.2"

[dependency-groups]
dev = [
    "colorlog>=6.12.0",
    "pytest-homeassistant-custom-component>=0.13.355",
    "ruff>=0.15.22",
]

[tool.uv]
package = false